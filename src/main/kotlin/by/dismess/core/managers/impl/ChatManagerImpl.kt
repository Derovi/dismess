package by.dismess.core.managers.impl

import by.dismess.core.managers.ChatManager
import by.dismess.core.model.Message
import by.dismess.core.utils.UniqID

class ChatManagerImpl : ChatManager {
    override fun sendMessage(user: UniqID, message: Message) {

    }
}
